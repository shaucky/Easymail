package easymail.core {
    import flash.events.EventDispatcher;
    
    /**
     * The MailClient class is the entry class for Easymail library.
     * <p>The MailClient is scoped so application can constructs numbers of MailClient objects.</p>
     * @playerversion AIR 51.0
     */
    public final class MailClient extends EventDispatcher {
        public function MailClient() {
            //
        }
    }
}